language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-jasmine-amd' ]; then cd .. && eval "mv $currentfolder generator-jasmine-amd" && cd generator-jasmine-amd; fi

